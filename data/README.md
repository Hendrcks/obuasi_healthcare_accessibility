# Data directory

The core analysis retrieves OpenStreetMap data when it runs, so raw downloads are not committed. Add independently verified municipal boundaries or healthcare-facility records here when they become available.

